Engulfing_ProfitExit Backtest reports (2012–2025)

This folder contains yearly backtest reports exported from cTrader. Open `index.html` to view the list of reports. Each report is a standalone HTML file located in the `gecmis/` subfolder.

For GitHub Pages: the `.nojekyll` file is included to prevent Jekyll processing so the reports are served as static files.
